import React from "react";
import ReactDOM from "react-dom/client";
import App from "./app";
import "./index.css"; // make sure this imports the new CSS

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);